<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="container p-4">

        <div class="row">
            <div class="col-md-4 mx-auto">
                <div class="card mt-4 text-center">
                    <div class="card-header">
                        Tienes que dirigirte AH !
                    </div>

                    <div class="mt-3">
                        <form action="/allasignaciones" method="POST">
                            <div class="container p-2">
                                <div class="row">
                                    <div class="col d-flex justify-content-center">
                                        <div class="card" id="mapacard">
                                            <div class="card-body" id="map-template">
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="mt-3">
                                <p>
                                    Recoger Usuario en:
                                    <input type="text"  value="{{listarAsignaciones.StartDirection}}" name="startDireccion" class="form-control">
                                </p>
                                
                            </div>
                            <input type="hidden" name="idRequest" value="{{listarAsignaciones.idRequest}}">
                            
                            <div class="mt-3">
                            <p>
                                    Destino User:
                                    <input type="text"  value="{{listarAsignaciones.FinalDirection}}" name="finalDireccion" class="form-control">
                                </p>        
                            </div>
                            <div class="mt-3">
                                <p>
                                    Referecias User:
                                    <input type="text"  value="{{listarAsignaciones.Descriptions}}" name="descripcion" class="form-control">
                                </p>   
                            </div>
                            <div class="mt-3">
                                <input type="hidden" name="fechaPeticion" value="${new Date()}">
                            </div>
                            <div class="mt-3">
                                <button class="btn btn-success btn-block w-100">
                                    Recibir Peticion
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
    <script src="/js/main.js"></script>
</body>
</body>

</html>